import { createGlobalStyle } from "styled-components";

export const ExplainStyles = createGlobalStyle`
strong {
  color: #ffd908;
}

a,
a:visited,
a:active {
  color: var(--teal);
  text-decoration: none;
}
`;
